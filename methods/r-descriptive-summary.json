{
  "contract_version": "data-lens-method-manifest/1.0",
  "method_id": "data_lens.r_descriptive_summary",
  "version": "0.1.0",
  "status": "experimental",
  "name": "R 数值列描述摘要",
  "question_types": ["description"],
  "accepted_units": ["table_row"],
  "input_shapes": ["csv"],
  "eligibility_checks": ["Rscript is available", "CSV has a header", "numeric columns are present"],
  "assumptions": ["the registered R script and input CSV use compatible numeric parsing rules"],
  "human_gates": ["confirm units and missing-value meaning before interpretation"],
  "implementation": {"runtime": "r", "entrypoint": "methods/implementations/r/descriptive_summary.R", "network_policy": "deny", "optional_dependencies": ["R >= 4.2"]},
  "outputs": ["per-column count", "missing count", "mean", "minimum", "maximum"],
  "diagnostics": ["R runtime availability, process return code, result-contract errors, and output hash"],
  "evidence_requirements": ["input file hash", "column name", "method result contract"],
  "allowed_claims": ["descriptive values computed from eligible rows"],
  "forbidden_claims": ["descriptive difference proves causality", "missing values are zeros"],
  "validation": {"status": "fixture_only", "fixtures": ["tests/fixtures/r_numeric.csv"]}
}
